# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Gitlab-CI-Demo' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Gitlab-CI-Demo
  pod 'SwiftLint'

  target 'Gitlab-CI-DemoTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Gitlab-CI-DemoUITests' do
    # Pods for testing
  end

end
